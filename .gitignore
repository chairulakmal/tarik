# Environment
.env
.env.local
.env.*.local

# tarik configuration
.tarik

# ── Ruby / Rails ───────────────────────────────────────────────────────────────
api/config/master.key
api/log/
api/tmp/
api/.bundle/
api/vendor/bundle/
api/public/system/
api/public/uploads/
api/.byebug_history
api/coverage/
*.log

# ── Next.js ────────────────────────────────────────────────────────────────────
frontend/.next/
frontend/out/
frontend/coverage/

# ── Node ───────────────────────────────────────────────────────────────────────
node_modules/
frontend/node_modules/

# ── OS ─────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Editor ─────────────────────────────────────────────────────────────────────
.idea/
*.swp
*.swo

# ── Agent instructions (tool-specific, not for template users) ─────────────────
# AGENTS.md IS committed — it's the primary, tool-agnostic agent context for template users.
# CLAUDE.md and CLAUDE.template.md are gitignored so they don't reach template users:
#   CLAUDE.md          — Claude Code-specific workflow overrides for tarik's own dev
#   CLAUDE.template.md — superseded by AGENTS.md; no longer distributed
CLAUDE.md

CLAUDE.template.md
.claude/
